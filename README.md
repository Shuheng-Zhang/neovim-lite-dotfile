# neovim-lite dotfile

基于 `nvim-lua/kickstart.nvim` 的自定义 Neovim 配置.

# 使用依赖

1. `Neovim`: 软件主体, 要求版本为 **_v0.11.x_**
2. `git`: git 工具链
3. `lazygit`: 用于 **_lazygit_** 面板
4. `Node`: 用于多个 LSP, 要求版本为 **v20** 或更新版本
5. `curl` 用于测试 **_HTTP/HTTPS_** 请求
6. ripgrep
7. bk (可选, 用于ePub浮窗阅读)
